# Quality evidence

The stable medium-suite identity is `xq-svctest-kit-medium`. Gradle's TestNG reports provide JUnit XML. CI uploads sanitized test results and logs and records the Hub Ticket/PR correlation. Medium tests use only localhost fixtures and Testcontainers.
